extends ../layout

block content
  p.text-center.question
    i #{givenName}
    | &nbsp;
    | has a unique type and talent and wants you to rate them.
  p.text-center.description #{givenName} type is #{fs.indefiniteArticle(profileType.name)} #{profileType.name} which means #{profileType.description}
  p.text-center.description
    b How correct is this?
  center(data-parsed="")
    table.spacer
      tbody
        tr
          td(height='30px', style='font-size:30px;line-height:30px;')
    table.button.rounded.large.float-center.small-expanded
      tr
        td
          table
            tr
              td
                a(href=evaluateLink, target='_blank') REVIEW ANONYMOUSLY
    table.spacer
      tbody
        tr
          td(height='30px', style='font-size:30px;line-height:30px;')
